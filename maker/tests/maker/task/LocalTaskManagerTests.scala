package maker.task

import org.scalatest.FunSuite

class LocalTaskManagerTests extends FunSuite{
  test("can create remote process and bring it down"){
    //val client = new LocalTaskManager
    //Thread.sleep(3000)
    //Thread.sleep(2000)
    //assert(manager.isRunning)
    //manager.stop
    //assert(! manager.isRunning)
  }
}
